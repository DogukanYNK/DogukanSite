namespace DogukanSite.Models
{
    // Kuponun indirim türünü belirtir
    public enum DiscountType
    {
        Percentage, // Yüzdesel (Örn: %15)
        FixedAmount // Sabit Tutar (Örn: 50 TL)
    }
}